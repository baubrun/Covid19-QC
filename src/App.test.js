import React from 'react'
import App from "./App"
import {shallow} from "enzyme"

jest.mock("./components/api")

describe('App component', () => {

    it('should Fetch data via mock', (done) => {
    });


});